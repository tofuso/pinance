# CLAUDE.md

検討内容は日本語で表記してください。

## プロジェクト概要

**Pinance** はPython製の家計簿ツールです。日本の金融機関が発行する銀行の取引データCSVを取り込み、取引データを保存し、口座の取引収支の時系列・期間別ビューや統計を提供します。
また、クレジットカードの明細CSVを取り込み、該当するクレジットカード取引の明細に詳細なクレジットカードの取引データを表示します。

機能の概要や要件は `README.md` に記載される。
また、機能や要件の追加に合わせて、 `README.md` も更新される。

## 開発環境のセットアップ

プロジェクトはPython仮想環境（`uv`）を使用します。ソースコード追加後の典型的なセットアップ手順：

```bash
python -m  uv  venv
source .venv/Scripts/activate   # Windows Git Bash
pip install -e .
```

## アーキテクチャ

### 計画中のレイヤー構成

1. **インポートレイヤー** — 銀行明細（三井住友銀行形式）およびクレジットカード取引（Vpass形式）のCSVパーサー。重複データの除外を行う
2. **ストレージレイヤー** — 口座残高・取引履歴の永続化。
3. **ビュー・クエリレイヤー** — 時系列一覧、日・週・月・年単位のページ閲覧、期間別の収支集計・比較、統計の表示

### CSVデータについて

- `.gitignore` により `*.csv` は除外されています — 金融CSVファイルはローカルのみで、コミットしないこと
- `csv/` には、下記のサンプルデータが含まれます。
  - 銀行の口座取引 `csv\meisai_202406-202408.csv`
  - クレジットカードの取引（1か月分） `csv\202604_sample.csv` 

### 構成

1. **フロントエンドプログラム** ー デフォルトではlocalhostにホストされ、ブラウザから集計データの閲覧や読み込みなどの指示を行う
2. **バックエンドプログラム** ー デフォルトではlocalhostにホストされ、フロントエンドからの指示に従い、データの集積や演算を行う